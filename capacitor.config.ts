import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tradetalent.app',
  appName: 'tradetalent',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
